# CLIProxyAPI / Sub2API JSON Converter

Browser-only converter for CLIProxyAPI and Sub2API JSON account formats.

Open `index.html`, paste or upload a JSON file, choose the target format, then copy or download the converted result.

All conversion happens locally in the browser.
